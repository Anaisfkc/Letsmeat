% if user is connect % affiche contenu

% else % affiche : veuillez vous connectez pour y accéder ..

{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil.id }}</td>
            </tr>
            <tr>
                <th>Saveurs</th>
                <td>{{ profil.saveurs }}</td>
            </tr>
            <tr>
                <th>Pratiquefood</th>
                <td>{{ profil.pratiquefood }}</td>
            </tr>
            <tr>
                <th>Prescriptionfood</th>
                <td>{{ profil.prescriptionfood }}</td>
            </tr>
            <tr>
                <th>Typefood</th>
                <td>{{ profil.typefood }}</td>
            </tr>
            <tr>
                <th>Recette</th>
                <td>{{ profil.recette }}</td>
            </tr>
            <tr>
                <th>Intolerance</th>
                <td>{{ profil.intolerance }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ profil.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_index') }}">back to list</a>

    <a href="{{ path('profil_edit', {'id': profil.id}) }}">edit</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}